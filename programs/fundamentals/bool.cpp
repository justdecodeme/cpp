#include <iostream>
using namespace std;

int main() 
{
    // In c++, when we print the value of a bool variable, 
    // it prints either 0 (for false) or 1 (for true). 
    // It doesn't print "true" or "false".
    
    cout << false; // 0    
    cout << true; // 1    
}